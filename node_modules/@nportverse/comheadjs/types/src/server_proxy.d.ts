export = server_proxy_t;
declare class server_proxy_t extends conn_t {
    constructor(url: string, port: number, xor: number|string);
    readonly url: string;
    readonly port: number;
    readonly state: string;
    connect(): Promise<server_proxy_t>;
    call(params: {tr_code:number|string}&Record<string,any>, resolve?: (rv: any)=>void, reject?: (e: unknown)=>void): Promise<any>;
    protected _return(packet: packet_t): void;
    protected reqs: Map<string, (rv: any)=>void>;
    protected resp_event: string;
    protected _uninit(): void;
    protected _challenge(packet: packet_t): any;
    protected _chal_result(packet: packet_t): any;
}
import conn_t = require("./conn");
import packet_t = require("./packet");
//# sourceMappingURL=server_proxy.d.ts.map
