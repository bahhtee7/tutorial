export = conn_t;
declare class conn_t extends EventEmitter {
    constructor(sock: net.Socket|undefined, xor: number|string);
    protected sock: net.Socket;
    protected xor: number;
    destroy(): void;
    send(tr_code: number|string, req_id: string, payloadOrError: Buffer|object|number, header?: Buffer|(p: packet_t)=>void): Promise<void>;
    protected _init(): void;
    protected ibuf: Buffer;
    protected _write(data: Buffer, resolve: any, reject: any): void;
    protected write(data: Buffer): Promise<void>;
    protected _proc_idata(): boolean;
}

import EventEmitter = require("events");
import net = require("net");
import packet_t = require("./packet");
//# sourceMappingURL=conn.d.ts.map
